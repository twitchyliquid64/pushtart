package util

import (
	"crypto/rand"
)

//RandAlphaKey returns a string of n characters containing only uppercase, lowercase letters and numbers.
func RandAlphaKey(n int) string {
	const alphanum = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz"
	var bytes = make([]byte, n)
	rand.Read(bytes)
	for i, b := range bytes {
		bytes[i] = alphanum[b%byte(len(alphanum))]
	}
	return string(bytes)
}
